import React from 'react';
import { View, StyleSheet } from 'react-native';
import FieldInput from './FieldInput.js';

function NewItemForm({ template, onChange }) {
  // data looks like [ { label, form, data }, ... ]
  return (
    <View></View>
  );
}